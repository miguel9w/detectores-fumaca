---
phase: 03-review-deploy
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - dist/index.html
  - gh-pages.log
  - package.json
  - vite.config.js
autonomous: true
requirements:
  - D-01
  - D-04
  - D-09
  - D-10
  - D-11
  - D-12
must_haves:
  truths:
    - 'Build succeeds and dist/index.html contains base "/detectores-fumaca/" in both script src and css href before publish (per D-10)'
    - 'vite.config.js base and package.json homepage both contain /detectores-fumaca/ and align with dist/index.html (per D-10 pitfall)'
    - 'npx gh-pages -d dist attempted: on success gh-pages branch has traceable commit and dist published; on failure log shows guidance and phase not blocked (per D-09/D-11/D-12)'
    - 'Remote origin verified or created guidance shown; gh auth status and git remote -v checked before publish (per D-12 pitfall)'
    - 'Playwright local suite still green after polish (no regressions) and screenshot artifact from Wave 1 persists (per D-01/D-04 graceful)'
  artifacts:
    - dist/index.html
    - gh-pages.log
    - test-results/deck-screenshot.png
  key_links:
    - 'vite build base /detectores-fumaca/ -> grep dist/index.html script+css -> npx gh-pages -d dist clone temp -> branch gh-pages push (D-10 gate)'
    - 'git remote origin existence -> gh-pages push success or D-12 avisa e nao bloqueia with guidance'
    - 'Wave 1 Playwright green -> Wave 2 build green -> deploy traceable commit (D-01 tudo+simulações vivas preserved through deploy)'
---

<objective>
Wave 2 Build e Deploy: gate build with base verification, verify remote origin, publish via npx gh-pages -d dist with traceable commit and failure guidance, and record final version artifact per D-09..D-12 and D-10 base gate.

Purpose: Ship live presentation to GitHub Pages project site https://miguel.github.io/detectores-fumaca/ with correct base path, leaving traceable commit and not blocking on token failures (per D-04 avisa e deploya and D-12).
Output: dist/index.html verified, gh-pages branch published (or failure guidance logged), build log, screenshot and console log retained for report.
</objective>

<execution_context>
@/home/miguel/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/miguel/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-review-deploy/03-CONTEXT.md
@.planning/phases/03-review-deploy/03-RESEARCH.md
@.planning/phases/03-review-deploy/03-PATTERNS.md
@.planning/phases/03-review-deploy/03-VALIDATION.md
@.planning/phases/03-review-deploy/03-01-SUMMARY.md
@vite.config.js
@package.json
@playwright.config.js
@tests/e2e/simulations.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build gate + base verification + remote origin check (D-10 pre-publish)</name>
  <files>dist/index.html, vite.config.js, package.json</files>
  <read_first>
    vite.config.js (base "/detectores-fumaca/"),
    package.json (homepage https://miguel.github.io/detectores-fumaca and deploy script),
    playwright.config.js (webServer url baseURL /detectores-fumaca/),
    .planning/phases/03-review-deploy/03-RESEARCH.md Verification base pipeline,
    .planning/phases/03-review-deploy/03-PATTERNS.md vite config pattern lines 338-355
  </read_first>
  <action>
    Execute D-10 Checar dist/index.html gate sequentially before any publish per PATTERNS.md Build->Verify->Deploy pipeline. Run npx vite build (must produce 1427 modules and dist/index.html 0.88kB). Immediately after, assert grep -q 'src="/detectores-fumaca/assets/' dist/index.html succeeds, and grep -q 'href="/detectores-fumaca/assets/' dist/index.html succeeds — if either fails, exit 1 with ERRO: dist/index.html sem base /detectores-fumaca/. Also cross-check grep -q detectores-fumaca vite.config.js package.json dist/index.html all three align; if package.json homepage diverges, fix to https://miguel.github.io/detectores-fumaca and base to /detectores-fumaca/ exactly.

    Then verify remote origin per RESEARCH Pitfall 1 and D-12: run git remote -v and jj git remote list (if jj exists) and gh auth status; capture outputs. If origin missing, do not fail — document guidance: git remote add origin https://github.com/miguel/detectores-fumaca.git (or repo name derived from gh repo view). Note that homepage suggests project site at github.com/miguel/detectores-fumaca — if gh repo view shows different, use actual repo and note decision. For this plan, keep D-12 avisa e nao bloqueia: missing remote is guidance not gate unless publish later needs it.

    Also capture build log to build.log via npx vite build 2>&1 | tee build.log and ensure dist/index.html exists. Verify Wave 1 Playwright still green quickly via npx playwright test --reporter=list (reuse). Do not modify vite.config.js unless base misaligned.

    This task is the gate: deploy task must not run until this gate passes (except origin missing is warning only).
  </action>
  <verify>
    <automated>npx vite build 2>&1 | tee build.log && echo "build done" && ls -lh dist/index.html && grep -q 'src="/detectores-fumaca/assets/' dist/index.html && echo "base script OK" || (echo "FAIL base script"; exit 1)</automated>
    <automated>grep -q 'href="/detectores-fumaca/assets/' dist/index.html && echo "base css OK" || (echo "FAIL base css"; exit 1)</automated>
    <automated>grep -q "detectores-fumaca" vite.config.js && grep -q "detectores-fumaca" package.json && echo "config base aligned OK"</automated>
    <automated>git remote -v; jj git remote list 2>&1 | head -20; gh auth status 2>&1 | head -20</automated>
    <automated>npx playwright test --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npx vite build exits 0 and dist/index.html exists with size >0
    - grep for src="/detectores-fumaca/assets/ in dist/index.html succeeds
    - grep for href="/detectores-fumaca/assets/ in dist/index.html succeeds
    - vite.config.js base equals "/detectores-fumaca/" and package.json homepage equals https://miguel.github.io/detectores-fumaca
    - git remote -v output captured and if empty, guidance for git remote add origin logged (not failing task)
    - npx playwright test --reporter=list still passes (Wave 1 hardened tests green)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Deploy via gh-pages with traceable commit and failure handling + final report (D-09/D-11/D-12/D-04/D-01/D-16)</name>
  <files>gh-pages.log, dist/index.html, test-results/deck-screenshot.png</files>
  <read_first>
    package.json deploy script (vite build && npx gh-pages -d dist),
    .planning/phases/03-review-deploy/03-RESEARCH.md gh-pages deploy with handling pattern,
    .planning/phases/03-review-deploy/03-PATTERNS.md package.json deploy pattern lines 376-386,
    tests/e2e/simulations.spec.js report screenshot test (D-16),
    .planning/phases/03-review-deploy/03-VALIDATION.md D-09/D-11/D-12 rows
  </read_first>
  <action>
    Implement D-09 npx gh-pages -d dist publish (per PATTERNS.md D-09 Tanto faz se subir, direct CLI). Do not create GitHub Actions workflow — use npm gh-pages 6.3.0 already installed. Run command as npx gh-pages -d dist 2>&1 | tee gh-pages.log and capture PIPESTATUS. Default gh-pages creates traceable commit on branch gh-pages with message Updates per D-11 — do not pass --no-history or --silent; keep default commit traceável.

    Implement D-12 Tenta gh-pages e avisa: if exit non-zero, parse gh-pages.log for origin not found, remote.origin.url, authentication failed, or permission errors. Print guidance: echo gh-pages falhou — verifique: git remote -v, gh auth status, permissões do repo and cat gh-pages.log and echo retry manual: git remote add origin <url> && npx gh-pages -d dist. Do NOT mark task failed or phase blocked — per D-04 avisa e deploya graceful degradation, log debt and continue. If missing remote, suggest git remote add origin https://github.com/miguel/detectores-fumaca.git (or URL from gh repo view if available). If gh auth shows miguel9w logged but push fails, note token scope read:org repo may need push permission.

    On success, verify via git branch -a | grep gh-pages or jj log showing gh-pages branch, and check git log on gh-pages branch shows recent Updates commit. Log gh-pages publishing summary.

    After deploy attempt (success or guided failure), ensure final report artifacts exist per D-16 Tudo+screenshot: test-results/deck-screenshot.png already created in Wave 1, build.log from Task 1, gh-pages.log from this task, and console log captured via Playwright report test. Tee outputs for professor deliverable.

    Use both git and jj for version control per ROADMAP Wave 2 tasks: commit polished files with conventional commit docs(03-02): build gate + deploy via gh-pages before publish, tag version if deploy succeeded.

    Preserve D-01 Tudo+simulações vivas: do not regress Wave 1 tests; final npx playwright test --reporter=list must remain green regardless of deploy push outcome.
  </action>
  <verify>
    <automated>cat gh-pages.log 2>&1 | head -100; echo "---"; git branch -a 2>&1 | grep -i gh-pages || echo "gh-pages branch not yet pushed (see log)"; git log --oneline -5 2>&1 | head -10</automated>
    <automated>ls -lh dist/index.html gh-pages.log test-results/deck-screenshot.png build.log 2>&1 | head -20</automated>
    <automated>npx playwright test --reporter=list 2>&1 | tail -20</automated>
    <automated>grep -q "detectores-fumaca" dist/index.html && echo "dist base still OK after deploy attempt"</automated>
  </verify>
  <acceptance_criteria>
    - npx gh-pages -d dist was attempted and gh-pages.log exists with output tee'd
    - If success: git branch -a shows gh-pages branch and log contains Published or similar; if failure: gh-pages.log contains error and stdout shows guidance git remote -v / gh auth status and retry instruction, phase not marked blocked per D-12
    - Traceable commit exists on gh-pages branch when push succeeded (git log on gh-pages shows Updates)
    - Final artifacts exist: dist/index.html with base OK, test-results/deck-screenshot.png with size>0, build.log, gh-pages.log
    - npx playwright test --reporter=list still green (no regressions from deploy)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| build→CDN gh-pages | dist artifact pushed via git to GitHub Pages CDN |
| cli→git remote | gh-pages uses git credential / token to push |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Spoofing | vite base path | low | mitigate | base fixed string /detectores-fumaca/ not user input, grep gate asserts before publish |
| T-03-04 | Tampering | gh-pages push | medium | mitigate | gh-pages 6.3.0 verified no postinstall, uses git token with repo scope, traceable commit preserves history |
| T-03-SC | Tampering | npm/pip installs | high | mitigate | No new installs this plan; package legitimacy audit approved vite/gh-pages/playwright |
</threat_model>

<verification>
- npx vite build && grep script and css base in dist/index.html (D-10 gate)
- npx gh-pages -d dist | tee gh-pages.log with success→ branch exists or failure→ guidance shown not blocked (D-09/D-11/D-12)
- npx playwright test --reporter=list still green and screenshot persists
- Final artifacts ls -lh dist/index.html gh-pages.log test-results/deck-screenshot.png build.log
</verification>

<success_criteria>
- Build gate passes with base verification in script and css before publish
- gh-pages publish attempted with traceable commit on success or clear avisa guidance on failure per D-12 without blocking phase
- All Wave 1 polish preserved, Playwright local suite still green, and report artifacts (screenshot+logs) ready for professor per D-16
</success_criteria>

<output>
Create .planning/phases/03-review-deploy/03-02-SUMMARY.md when done
</output>
