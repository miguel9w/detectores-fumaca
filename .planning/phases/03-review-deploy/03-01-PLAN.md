---
phase: 03-review-deploy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/App.jsx
  - src/utils/constants.jsx
  - src/components/ui/figure.jsx
  - tests/e2e/simulations.spec.js
  - test-results/deck-screenshot.png
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-13
  - D-14
  - D-15
  - D-16
must_haves:
  truths:
    - 'Autoria shows "Miguel Pandini Bett, 1 ano ensino médio" in Slide Autoria and SLIDE_CONFIG, zero remaining "Estudante" (per D-05)'
    - 'Figure fallback renders inline SVG diagram (He²⁺ Z=2 1s⁰ with nucleus + orbit) when src missing or onError, keeps aspectRatio and aria-label, not just gradient (per D-06)'
    - 'Chemistry constants ION_PROPERTIES (Z=2, protons 2, neutrons 2, electrons 0, charge +2, 1s⁰) match visible slide text in App.jsx/SLD-02/SLD-03 and ChemicalInfo (per D-03)'
    - 'All 7 slides reviewed for orthography, spacing, contrast and revista tokens #fefcf8 #0e4a7a #c9a227 #1a2a3a consistent (per D-07) and References retain 5 items formatted (per D-08)'
    - 'Both simulations auto-play total without click: 3D useFrame orbit moves on slide open and 2D Matter chamber auto-emits via setInterval+requestAnimationFrame (per D-02) — verified by Playwright screenshot diff / gauge poll'
    - 'Playwright local only (localhost:4173) finds exactly 2 canvases in DOM via Tabs hidden CSS (one visible, one hidden but attached) per D-13/D-15'
    - 'Playwright console check captures error AND warning (msg.type warning not warn) plus pageerror, allowlist only Download the React DevTools, critical warnings fail per D-14'
    - 'Report artifacts exist: test-results/deck-screenshot.png + build log + console log attached, definition of done Tudo+simulações vivas (per D-01/D-16) with graceful degradation noted (per D-04)'
  artifacts:
    - src/components/App.jsx
    - src/utils/constants.jsx
    - src/components/ui/figure.jsx
    - tests/e2e/simulations.spec.js
    - test-results/deck-screenshot.png
  key_links:
    - 'App.jsx Slide Autoria <-> constants.jsx SLIDE_CONFIG presenter/turma must both show Miguel Pandini Bett (D-05 cross-check)'
    - 'Figure onError -> HeliumDiagramSVG inline SVG preserves aspectRatio wrapper (D-06)'
    - 'TabsContent hidden CSS <-> Playwright evaluate canvas count ===2 + toBeAttached/toBeHidden (D-15 preservation)'
    - 'useFrame elapsedTime + Matter setInterval(900/1400) -> Playwright auto-play screenshot diff without paused gate (D-02)'
    - 'vite build dist/index.html -> Playwright webServer localhost:4173/detectores-fumaca/ -> gh-pages later (D-10 gate)'
---

<objective>
Wave 1 Testes e Revisão e Polish: apply all content polish (autoria, chemistry, revista, references, SVG fallback) and harden E2E verification (2 canvases fixos, warnings, auto-play, screenshot report) per D-01..D-08 and D-13..D-16 so deck is deploy-ready.

Purpose: Deliver Tudo + simulações vivas definition of done (D-01) with graceful degradation note (D-04) before build gate.
Output: Polished source files (App.jsx, constants.jsx, figure.jsx) and hardened tests/e2e/simulations.spec.js that pass locally (npx vite build + npx playwright test --reporter=list) with screenshot artifact.
</objective>

<execution_context>
@/home/miguel/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/miguel/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-review-deploy/03-CONTEXT.md
@.planning/phases/03-review-deploy/03-RESEARCH.md
@.planning/phases/03-review-deploy/03-PATTERNS.md
@.planning/phases/03-review-deploy/03-VALIDATION.md
@src/utils/constants.jsx
@src/components/App.jsx
@src/components/ui/figure.jsx
@src/components/ui/tabs.jsx
@src/components/AtomicStructure3D.jsx
@src/components/ParticleSimulation2D.jsx
@src/components/SimulationsIntegration.jsx
@src/components/ChemicalInfo.jsx
@src/components/References.jsx
@tests/e2e/simulations.spec.js
@playwright.config.js
@vite.config.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Autoria + chemistry + revista revisão completa (D-03/D-05/D-07/D-08)</name>
  <files>src/components/App.jsx, src/utils/constants.jsx</files>
  <read_first>
    src/components/App.jsx lines 155-172 (Slide Autoria current Estudante / Quimica Geral),
    src/utils/constants.jsx lines 33-39 (SLIDE_CONFIG presenter/turma),
    src/utils/constants.jsx lines 1-12 (ION_PROPERTIES),
    src/components/ChemicalInfo.jsx (how constants consumed),
    src/components/References.jsx (5 items list),
    src/components/Slide.jsx (revista wrapper tokens)
  </read_first>
  <action>
    Implement D-05 exactly: replace placeholder autoria in both files (per PATTERNS.md D-05 pattern). In src/components/App.jsx line 161 change Estudante to Miguel Pandini Bett, line 162 change Turma: Química Geral to 1 ano ensino médio, keep third line Disciplina but update or remove Turma prefix so no Estudante remains. In src/utils/constants.jsx change SLIDE_CONFIG presenter to Miguel Pandini Bett and turma to 1 ano ensino médio. No other file should retain Estudante.

    Implement D-03 chemistry accuracy cross-check: verify src/utils/constants.jsx ION_PROPERTIES holds symbol He²⁺, atomicNumber 2, protons 2, neutrons 2, electrons 0, charge +2, electronConfiguration 1s⁰ — compare visible text in App.jsx SLD-02/SLD-03 (Z=2, Cátion +2, 1s²→1s⁰) and ChemicalInfo.jsx renders Z, +charge, 1s⁰. Do not change constants values unless mismatch; ensure grep for He²⁺, Z=2, 1s⁰ finds consistent hits in App.jsx and constants.jsx.

    Implement D-07 revisão completa: review all 7 slides in App.jsx for orthography, spacing, contrast, consistent revista tokens (#fefcf8 bg, #0e4a7a blue, #c9a227 gold, #1a2a3a ink) and SLIDE_CONFIG title. Keep tokens hardcoded hex as in codebase, do not introduce new colors. Ensure Deck theme colors align with tailwind.css @theme tokens and SimulationsIntegration headings. Fix any spacing or typo but do not alter slide count or order.

    Preserve D-08: References.jsx must keep exactly 5 items (Rutherford 1911, NFPA 2020, IAEA, Gomes 2019, Geiger 1909) — only formatting polish allowed, no item add/remove.

    Graceful degradation note per D-04: if minor visual slip found, note in code comment debt rather than blocking.

    Verification anchors: after edits, grep -r Estudante src/ must return 0; grep -r Miguel Pandini Bett src/ must return 2 hits (App.jsx + constants.jsx); grep -n He²⁺ src/components/App.jsx src/utils/constants.jsx must align.
  </action>
  <verify>
    <automated>grep -r "Estudante" src/ | wc -l | xargs test 0 -eq && echo "no Estudante OK" || (echo "FAIL Estudante remains"; exit 1)</automated>
    <automated>test $(grep -r "Miguel Pandini Bett" src/ | wc -l) -eq 2 && echo "autoria 2 hits OK" || (echo "FAIL autoria count"; grep -r "Miguel Pandini Bett" src/; exit 1)</automated>
    <automated>grep -n "Z=2\|1s⁰\|He²⁺" src/components/App.jsx src/utils/constants.jsx | head -20</automated>
    <automated>npx vite build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -r Estudante src/ returns 0 lines
    - grep -r Miguel Pandini Bett src/ returns exactly 2 lines (App.jsx Slide Autoria + constants.jsx SLIDE_CONFIG)
    - grep -n He²⁺|Z=2|1s⁰ across App.jsx and constants.jsx shows consistent Z=2, charge +2, 1s⁰ matching ION_PROPERTIES
    - npx vite build succeeds (1427 modules) without errors
    - References.jsx still contains exactly 5 li items with Rutherford/NFPA/IAEA/Gomes/Geiger
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Figure SVG diagrama fallback (D-06) + preserve aspectRatio and a11y</name>
  <files>src/components/ui/figure.jsx</files>
  <read_first>
    src/components/ui/figure.jsx (41 lines current gradient fallback),
    src/components/App.jsx Figure usages (4 slides with src),
    .planning/phases/03-review-deploy/03-PATTERNS.md Figure section (target HeliumDiagramSVG pattern),
    .planning/phases/03-review-deploy/03-RESEARCH.md Pattern 3 (SVG fallback code)
  </read_first>
  <action>
    Implement D-06 per PATTERNS.md target pattern: replace gradient + ◈ fallback in src/components/ui/figure.jsx with inline SVG diagram fallback. Add HeliumDiagramSVG sub-component before Figure export rendering vector diagram: viewBox 0 0 200 120, circle nucleus fill #c9a227 stroke #0e4a7a, two inner proton circles fill #ff6b6b at (93,56) and (107,64) radius 6, ellipse orbit dashed rx 42 ry 42 stroke #0e4a7a strokeDasharray 4 3 opacity 0.25, text He²⁺ Z=2 +2 at (100,88) fontSize 7 fill #0e4a7a weight 700 centered, text 1s⁰ — sem elétrons at (100,98) fontSize 6 fill #1a2a3a opacity 0.6, svg className w-full h-full role img aria-label Diagrama He²⁺.

    Update Figure fallback branch: when showFallback (!src || error) render div absolute inset-0 flex flex-col items-center justify-center bg-[#fefcf8] p-4 containing HeliumDiagramSVG + p text-xs showing alt or Ilustração He²⁺. Keep wrapper div style aspectRatio aspect unchanged. Keep onError handler on img as setError true. Keep figcaption pattern border-t border-[#c9a227]/10 with caption and credit italic. Do not add retry loop — SVG inline never triggers onError loop so guard not needed. Keep imports useState.

    Preserve D-04 graceful degradation: Figure fallback must not block Deck navigation.

    After edit, ensure no residual gradient string remains in fallback branch.
  </action>
  <verify>
    <automated>grep -n "HeliumDiagramSVG\|aria-label.*Diagrama" src/components/ui/figure.jsx && echo "SVG OK" || (echo "FAIL SVG missing"; exit 1)</automated>
    <automated>grep -n "aspectRatio" src/components/ui/figure.jsx | head -5</automated>
    <automated>npx vite build 2>&1 | tail -20</automated>
    <automated>npx playwright test --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/figure.jsx contains HeliumDiagramSVG function and svg with role img aria-label Diagrama He²⁺ and viewBox 0 0 200 120
    - Fallback div uses bg-[#fefcf8] p-4 (not gradient-to-br) and preserves style aspectRatio aspect
    - img retains onError={() => setError(true)} and loading lazy
    - npx vite build succeeds
    - Existing Playwright tests still pass (no regression from fallback change)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Harden Playwright E2E — warnings, 2 canvases fixos, auto-play, screenshot report (D-02/D-13/D-14/D-15/D-16 + D-01/D-04)</name>
  <files>tests/e2e/simulations.spec.js</files>
  <read_first>
    tests/e2e/simulations.spec.js (77 lines current 3 tests),
    playwright.config.js (webServer port 4173 baseURL /detectores-fumaca/),
    src/components/ui/tabs.jsx (TabsContent hidden CSS),
    src/components/AtomicStructure3D.jsx (useFrame ref mutation),
    src/components/ParticleSimulation2D.jsx (setInterval 900/1400 + rAF draw + Gauge current µA)
  </read_first>
  <action>
    Harden tests/e2e/simulations.spec.js to cover all Wave 1 verification gaps per RESEARCH.md Validation Architecture and PATTERNS.md analog fixes. Keep existing build passes test but expand D-10 base check: after execSync npx vite build, assert dist/index.html exists AND contains src="/detectores-fumaca/assets/ and href="/detectores-fumaca/assets/ via grep/fs read.

    Fix D-15 canvas count: change expect(canvasCount).toBeGreaterThanOrEqual(2) to expect(canvasCount).toBe(2) exactly. Keep evaluate querySelectorAll canvas length. Add D-15 hidden vs visible distinctions: after navigating 7 ArrowRight presses + waits, assert via locators: page.locator('[role="tabpanel"]') has count 2, page.locator('[role="tabpanel"]:not([hidden]) canvas') isVisible, page.locator('[role="tabpanel"][hidden] canvas') isAttached and isHidden (keeps WebGL alive via CSS not unmount). Log canvas details for debugging.

    Fix D-14 console warnings: collect both msg.type error and warning (note type is warning not warn) plus pageerror into arrays, do NOT throw inside page.on callbacks (anti-pattern #29214). After navigation, filter with allowlist [/Download the React DevTools/] only, then assert pageErrors toEqual [], consoleErrors filtered toEqual [], criticalWarnings toEqual [].

    Add D-02 auto-play total test: new test titled auto-play total — both simulations animate without click D-02. Navigate to simulations slide (7 ArrowRight), wait 2s, capture gauge current µA text or canvas pixel. Wait 1000ms, re-read gauge value or take two screenshots with delay and compare bytes not equal, assert current changed or screenshot diff. For 3D, polling via evaluate that canvas context exists and useFrame moves electron (indirect via second screenshot diff). No paused gate or IntersectionObserver reintroduction.

    Add D-06 Figure fallback test: new test Figure SVG fallback when src fails D-06. Goto /, evaluate to inject bad src on first Figure img or mount check: set page.evaluate to create Figure with invalid src and assert svg[aria-label="Diagrama He²⁺"] becomes visible and fallback wrapper contains He²⁺ Z=2 text. Alternatively use page.route to abort image requests and assert fallback visible.

    Add D-16 report screenshot test: new test report — screenshot + logs D-16. On page load, collect logs via page.on console, navigate slides, wait 2s, then page.screenshot({path: "test-results/deck-screenshot.png", fullPage: true}) and testInfo.attach deck-screenshot and console-log body logs.join("\n"). Ensure test-results dir exists. Build log captured via execSync in build passes test; console log via this test.

    Keep all tests using baseURL /detectores-fumaca/ and webServer localhost:4173 per D-13 Só Playwright local — do not add live URL. Ensure 6-7 tests total run in ~15s. Update 03-VALIDATION.md Wave 0 checklist intent via tasks.

    Do not introduce IntersectionObserver gate or active && Canvas conditional — preserve Tabs always mounted per D-15.
  </action>
  <verify>
    <automated>npx vite build 2>&1 | tail -20 && grep -q 'src="/detectores-fumaca/assets/' dist/index.html && echo "base script OK" && grep -q 'href="/detectores-fumaca/assets/' dist/index.html && echo "base css OK"</automated>
    <automated>npx playwright test --reporter=list 2>&1 | tail -50</automated>
    <automated>ls -lh test-results/deck-screenshot.png 2>&1 | head -5; grep -q "detectores-fumaca" vite.config.js package.json dist/index.html && echo "base aligned OK"</automated>
    <automated>grep -c "toBe(2)" tests/e2e/simulations.spec.js; grep -c "warning" tests/e2e/simulations.spec.js; grep -c "deck-screenshot" tests/e2e/simulations.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - npx vite build succeeds and dist/index.html contains src="/detectores-fumaca/assets/ and href="/detectores-fumaca/assets/
    - npx playwright test --reporter=list passes all tests (build passes, canvas count ===2 with hidden/attached assertions, no console errors AND no critical warnings, auto-play diff passes, Figure SVG fallback visible, report screenshot created)
    - tests/e2e/simulations.spec.js contains msg.type === "warning" capture and allowlist for Download the React DevTools, uses evaluate canvas length ===2 not >=2, distinguishes hidden vs visible via role tabpanel
    - test-results/deck-screenshot.png exists with size >0 and is attachable
    - No IntersectionObserver reintroduced and no active && Canvas conditional
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→Figure src | External image URL (Wikimedia) crosses into DOM via img src |
| browser→Canvas/WebGL | WebGL context creation from client GPU |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing | Figure src prop | low | mitigate | src is string URL only, React escapes, onError guard no eval, SVG fallback inline not img |
| T-03-02 | Tampering | npm install (vite/gh-pages/playwright) | high | mitigate | No new packages in this plan; existing audit Verified 11yrs/800k wk gh-pages, playwright 8M/wk, all approved |
| T-03-SC | Tampering | npm/pip installs | high | mitigate | Package legitimacy audit exists — no installs this wave, slopcheck not needed |
</threat_model>

<verification>
- Wave 1 gate: npx vite build && grep -q 'src="/detectores-fumaca/assets/' dist/index.html && grep -q 'href="/detectores-fumaca/assets/' dist/index.html
- grep -r Estudante src/ ==0 && grep -r Miguel Pandini Bett src/ ==2
- npx playwright test --reporter=list must be 5-7/5-7 green (includes new warnings, canvas ===2, auto-play, fallback, screenshot)
- test-results/deck-screenshot.png exists and fullPage screenshot captured
- Overall D-01 Tudo+simulações vivas satisfied when all above green
</verification>

<success_criteria>
- Autoria replaced in both files, zero Estudante, build green, chemistry cross-check He²⁺ Z=2 1s⁰ matches constants
- Figure.svg fallback visible on src failure preserving aspectRatio and a11y
- E2E hardened for warnings + exactly 2 canvases via hidden CSS + auto-play + screenshot report all passing locally per D-13/D-14/D-15/D-16
- All 7 slides revista consistent per D-07 and 5 references retained per D-08
</success_criteria>

<output>
Create .planning/phases/03-review-deploy/03-01-SUMMARY.md when done
</output>
