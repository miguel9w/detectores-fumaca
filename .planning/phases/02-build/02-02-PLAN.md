---
phase: 02-build
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - package.json
  - vite.config.js
  - index.html
  - src/index.jsx
  - src/styles/slide-styles.css
  - src/styles/tailwind.css
  - src/lib/utils.js
  - src/components/ui/tabs.jsx
  - src/components/ui/figure.jsx
  - src/components/Slide.jsx
  - src/components/App.jsx
  - src/components/SimulationsIntegration.jsx
  - src/components/AtomicStructure3D.jsx
  - src/components/ParticleSimulation2D.jsx
  - src/utils/constants.jsx
  - tests/e2e/simulations.spec.js
  - playwright.config.js
autonomous: true
requirements: [SLD-01, SLD-02, SLD-03, SLD-04, SLD-05, SLD-06, SLD-07, SLD-08, SLD-09, SIM-01, SIM-02]
must_haves:
  truths:
    - "Simulações 3D e 2D são visíveis imediatamente ao navegar para o slide de simulações — sem IntersectionObserver gate"
    - "Ambas simulações permanecem montadas e alternam via tabs 3D|2D (não desmontam)"
    - "Elétrons orbitam o núcleo em AtomicStructure3D (animação contínua via useFrame, não estáticos)"
    - "ParticleSimulation2D exibe câmara de ionização interativa (partículas de fumaça ionizáveis, íons He²⁺/elétrons, interação por clique/drag)"
    - "Visual revista científica ilustrada aplicado em todos os slides (fundo #fefcf8, azul #0e4a7a, dourado #c9a227, tipografia serif display + sans body)"
    - "Fotos reais com legenda (Figure) presentes nos slides de conteúdo (detector, Rutherford, He²⁺)"
    - "npx vite build passa sem erros e Playwright confirma >=2 canvas visíveis + zero console errors críticos"
  artifacts:
    - "src/lib/utils.js exporta cn() via clsx + tailwind-merge"
    - "src/styles/tailwind.css com @import tailwindcss e tokens revista"
    - "src/components/Slide.jsx wrapper estilizado"
    - "src/components/SimulationsIntegration.jsx sem IntersectionObserver, com tabs"
    - "src/components/AtomicStructure3D.jsx com useFrame orbit + labels + lights"
    - "src/components/ParticleSimulation2D.jsx câmara de ionização interativa"
    - "src/components/ui/figure.jsx + src/components/ui/tabs.jsx"
    - "playwright.config.js + tests/e2e/simulations.spec.js"
  key_links:
    - "vite.config.js -> @tailwindcss/vite plugin integrado ao pipeline Vite"
    - "Slide.jsx -> Deck (Spectacle) mantém orquestrador, apenas decora filhos"
    - "SimulationsIntegration -> AtomicStructure3D + ParticleSimulation2D sempre montadas, tabs controlam visibilidade via CSS (não unmount)"
    - "AtomicStructure3D useFrame -> electron position atualizada a 60fps sem useState"
    - "ParticleSimulation2D requestAnimationFrame -> Matter.Engine.update + canvas 2D draw sincronizados"
---

<objective>
Fix Wave 2 Build: recuperar simulações invisíveis (root cause IntersectionObserver + Spectacle display:none), rebuild visual revista científica mantendo Spectacle Deck como orquestrador, atualizar dependências para latest compat e verificar via build + Playwright.

Purpose: Desbloquear entrega — simulações são diferenciadores P2/SIM-01/SIM-02 e UI atual é bare/unstyled. Approach C escolhido: Tailwind/shadcn visual rebuild sem reescrever Deck.
Output: Deck estilizado revista, tabs 3D|2D sempre montadas, AtomicStructure3D orbitando, ParticleSimulation2D câmara interativa, build verde, E2E passando.
</objective>

<execution_context>
@/home/miguel/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/miguel/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-build/RESEARCH.md
@.planning/phases/02-build/SUMMARY.md
@src/components/App.jsx:7
@src/components/Slide.jsx:3
@src/components/SimulationsIntegration.jsx:5
@src/components/AtomicStructure3D.jsx:31
@src/components/ParticleSimulation2D.jsx:10
@src/styles/slide-styles.css:1
@src/utils/constants.jsx:1
@vite.config.js:6
@index.html:2
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependency update + Tailwind/shadcn setup</name>
  <files>package.json, vite.config.js, src/styles/tailwind.css, src/lib/utils.js, src/index.jsx, index.html</files>
  <action>
    Bump dependências para latest compat per decisão (manter matter-js 0.20.x):
    - react 19.2.x (já em ^19.2.8, confirmar latest 19.2.x), react-dom 19.2.x
    - @react-three/fiber 9.x (9.7.0 -> latest 9.x), @react-three/drei 10.x (10.7.8 -> latest 10.x), three 0.185.x -> latest 0.185+, three-stdlib 2.36.x kept
    - vite 8.x (8.2.2 -> latest 8.x), @vitejs/plugin-react 6.1.1 -> latest, spectacle 10.2.3 pinned, styled-components 6.5.3 kept (peer de spectacle)
    - ADD: tailwindcss 4.1.x, @tailwindcss/vite 4.1.x, clsx latest, tailwind-merge latest

    Passos:
    1. npm install tailwindcss@latest @tailwindcss/vite@latest clsx tailwind-merge -D ou deps per Tailwind 4.1 docs (@tailwindcss/vite é plugin Vite-first, não precisa tailwind.config.js em v4 — usar CSS-first config)
    2. Criar src/lib/utils.js exportando cn(...inputs) = twMerge(clsx(inputs)) — padrão shadcn.
    3. Criar src/styles/tailwind.css com: @import "tailwindcss"; @theme { --color-revista-bg: #fefcf8; --color-revista-blue: #0e4a7a; --color-revista-gold: #c9a227; --color-revista-ink: #1a2a3a; --font-display: "Fraunces", "DM Serif Display", serif; --font-body: "Inter", "DM Sans", sans-serif; } + base layer: body bg-[#fefcf8] text-[#1a2a3a].
    4. Em vite.config.js:6 — importar tailwindcss from "@tailwindcss/vite" e adicionar ao array plugins: [react(), tailwindcss()] — manter alias react/react-dom e optimizeDeps existentes; remover input:"./src/index.jsx" se conflitar (Vite resolve entry via index.html:2); manter base:"/detectores-fumaca/" e external.
    5. Em src/index.jsx — importar "./styles/tailwind.css" antes de App; manter createRoot flow.
    6. Em index.html:2 — adicionar <link> Google Fonts: Fraunces + Inter/DM Sans (display=swap) no <head>; lang pt-BR já ok.
    7. Remover ou deprecar src/styles/slide-styles.css:1 — manter arquivo mas esvaziar/transformar em thin compat layer que apenas importa tailwind (evitar FOUC). Não deletar abruptamente se App importa.
    8. Rodar npm install e npx vite build para validar que Vite 8 + @tailwindcss/vite + react 19.2 + r3f 9 coexistem (checar peer warnings).
    Por que: Tailwind 4.1 via @tailwindcss/vite é o caminho oficial Vite (sem PostCSS config necessária), clsx+twMerge é padrão shadcn para cn().
    Evitar: tailwind.config.js legado em v4, PostCSS manual, downgrade react para 18 (quebraria 19.2 já em uso).
  </action>
  <verify>
    <automated>npm ls --depth=0 2>&1 | grep -E "react|vite|tailwind|three|matter" ; npx vite build 2>&1 | tail -n 50</automated>
  </verify>
  <done>package.json reflete bumps (react 19.2.x, fiber 9.x, drei 10.x, three 0.185+, vite 8.x, + tailwindcss 4.1, @tailwindcss/vite, clsx, tailwind-merge); vite.config.js contém tailwindcss() plugin; src/lib/utils.js exporta cn; src/styles/tailwind.css existe com tokens #fefcf8/#0e4a7a/#c9a227; npx vite build passa sem erro.</done>
</task>

<task type="auto">
  <name>Task 2: Slide.jsx revista wrapper + theme tokens + UI primitives</name>
  <files>src/components/Slide.jsx, src/components/ui/tabs.jsx, src/components/ui/figure.jsx, src/utils/constants.jsx, src/styles/tailwind.css</files>
  <action>
    Rebuild visual mantendo Spectacle Deck como orquestrador (Approach C):
    1. Slide.jsx:3 — reescrever wrapper que envolve SpectacleSlide mas aplica revista styling via div interno (não tentar estilizar Deck root). Estrutura: <SpectacleSlide ...><div className="revista-slide min-h-full bg-[#fefcf8] text-[#1a2a3a]"> <header> se title prop -> badge dourado + h2 serif + linha decorativa; children em <div className="revista-content max-w-5xl mx-auto px-8 py-6">. Usar tokens Tailwind: bg-[#fefcf8], text-[#0e4a7a] para headings, border-[#c9a227]/20, accent text-[#c9a227]. Tipografia: headings font-display (Fraunces), body font-body. Adicionar decoração sutil: corner ornament (border-2 border-[#c9a227]/15 rounded-2xl) e footer line com "Revista Científica Ilustrada — He²⁺". Manter speakerNotes passthrough. Export default.
    2. src/components/ui/tabs.jsx — criar Tabs primitive shadcn-style sem Radix (evitar dep extra): export Tabs, TabsList, TabsTrigger, TabsContent. Tabs usa useState value default "3d"; TabsList é pill com bg-[#0e4a7a] text-white, TabsTrigger active bg-[#c9a227] text-[#0e4a7a]; TabsContent renderiza children sempre montados mas toggla via hidden/className (display:none vs block) — CRÍTICO: não unmount, apenas CSS toggle, para manter Canvas/WebGL contextos vivos. A11y: role tablist/tab.
    3. src/components/ui/figure.jsx — criar Figure para fotos reais com legenda (SLD-04): props {src, alt, caption, credit, aspect="4/3"}. Render <figure className="revista-figure rounded-xl overflow-hidden border border-[#c9a227]/20 shadow-sm bg-white"><img className="w-full object-cover" /><figcaption className="px-4 py-3 text-sm text-[#1a2a3a]/70 bg-[#fefcf8] border-t border-[#c9a227]/10">caption + credit. Fallback se src vazio: placeholder gradient com ícone.
    4. src/utils/constants.jsx:1 — estender SLIDE_CONFIG com theme tokens: revista: { bg:"#fefcf8", blue:"#0e4a7a", gold:"#c9a227", ink:"#1a2a3a" } para single-source; manter ION_PROPERTIES intacto.
    5. src/styles/tailwind.css — adicionar componentes revista: .revista-slide, .revista-kicker (uppercase tracking-widest text-[#c9a227]), .revista-title, .revista-rule (hr dourada).
    Evitar: tentar theme prop do Spectacle (limitado), inline styles hardcoded fora de tokens, Radix dependency (desnecessária).
    Referência: manter Spectacle Deck intacto — Slide.jsx apenas decora conteúdo interno, não substitui Deck.
  </action>
  <verify>
    <automated>grep -n "revista-slide\|#fefcf8\|#0e4a7a\|#c9a227\|Tabs\|Figure" src/components/Slide.jsx src/components/ui/tabs.jsx src/components/ui/figure.jsx 2>&1 | head -n 40</automated>
  </verify>
  <done>Slide.jsx aplica wrapper revista com tokens #fefcf8/#0e4a7a/#c9a227, tipografia display/body, ornament; Tabs primitive existe e usa CSS visibility (não unmount); Figure existe com legenda; constants expõe tokens; nenhum erro de import.</done>
</task>

<task type="auto">
  <name>Task 3: SimulationsIntegration tabs fix — remover IntersectionObserver gate</name>
  <files>src/components/SimulationsIntegration.jsx</files>
  <action>
    Fix root cause confirmado: SimulationsIntegration.jsx:5 useInView com IntersectionObserver threshold 0.5 dentro de Spectacle Deck (slides hidden via display:none/translateX) => mounted nunca true => simulação nunca aparece. Solução: remover completamente useInView/mounted gate e adotar tabs 3D|2D sempre montadas per Approach C.
    Implementar:
    1. Deletar useInView hook (linhas 5-24) e estados mounted/inView. Manter apenas webglAvailable check (linhas 29-33) mas melhorar: testar canvas.getContext('webgl2') || canvas.getContext('webgl') além de window.WebGLRenderingContext.
    2. Importar Tabs primitives de "@/components/ui/tabs" ou relativo; usar Tabs defaultValue "3d" com TabsList contendo Trigger "Visão 3D — Estrutura Atômica" e "Visão 2D — Câmara de Ionização".
    3. Estrutura: <div className="sim-tabs w-full"><Tabs ...><TabsList/><TabsContent value="3d"><div className="h-[420px] bg-white rounded-xl border border-[#c9a227]/20 overflow-hidden"><AtomicStructure3D/></div><p className="text-xs text-center mt-2 text-[#1a2a3a]/60">Modelo 3D interativo — arraste para orbitar</p></TabsContent><TabsContent value="2d"> similar para ParticleSimulation2D. Ambas TabsContent SEMPRE renderizadas no DOM (hidden via CSS, não condicional). Remover if (!mounted) return <div ref={ref} .../>.
    4. Estilizar container: header com badge "Simulações Interativas" + título "Do Átomo ao Detector" + subtítulo explicativo; tabs pill em blue/gold.
    5. Fallback no-webgl: manter mas estilizado revista (bg-[#fefcf8] border dourado) em vez de inline style cru.
    6. Garantir ref removido — não mais necessário observer; div root sem ref.
    Evitar: reintroduzir IntersectionObserver, lazy mount condicional, display:none no parent do Canvas que zera tamanho (usar hidden attribute + CSS que mantém layout mas esconde; ou visibility). Para Canvas, usar CSS block/hidden mas manter dimensões via h-[420px] fixo.
    Teste manual: navegar para slide Simulações — ambas tabs devem existir no DOM imediatamente (inspecionar).
  </action>
  <verify>
    <automated>grep -n "IntersectionObserver\|useInView\|inView\|mounted\|threshold" src/components/SimulationsIntegration.jsx; echo "---tabs check---"; grep -n "Tabs\|TabsContent\|AtomicStructure3D\|ParticleSimulation2D" src/components/SimulationsIntegration.jsx</automated>
  </verify>
  <done>SimulationsIntegration.jsx não contém IntersectionObserver/useInView/mounted gate (grep retorna 0); contém Tabs com 2 TabsContent sempre montadas contendo AtomicStructure3D e ParticleSimulation2D; fallback WebGL estilizado; navegação Spectacle não bloqueia render.</done>
</task>

<task type="auto">
  <name>Task 4: AtomicStructure3D — órbita contínua + labels + luzes revista</name>
  <files>src/components/AtomicStructure3D.jsx</files>
  <action>
    Fix: atualmente AtomicStructure3D.jsx:31 Scene estática — electrons em posições fixas [0.8,0,0] e [-0.8,0,0], sem animação; luzes fracas; sem labels; sem useFrame.
    Reimplementar:
    1. Imports: adicionar useRef, useFrame from "@react-three/fiber", Text, Html from "@react-three/drei" (drei 10.x Text já suporta troika).
    2. Nucleus: manter sphereGeometry 0.3 mas melhorar material: color #c9a227 (gold) metalness 0.3 roughness 0.3 emissive #c9a227 emissiveIntensity 0.15; adicionar 2 prótons visuais internos (pequenas esferas #ff6b6b offset) + label Html/ Text "Núcleo: 2p⁺".
    3. Electron: refatorar para animado via useFrame. Criar ElectronOrbitAnimado: recebe radius, speed, phase, color. Internamente useRef no group, useFrame((state)=>{ t=state.clock.elapsedTime*speed + phase; ref.current.position.set(Math.cos(t)*radius, 0, Math.sin(t)*radius)}). Usar 2 elétrons com fases opostas (0 e Math.PI) para 1s orbital; speeds levemente diferentes para evitar sincronia perfeita. Manter ElectronOrbit ring como mesh com ringGeometry radius.
    4. Cena completa: <Canvas camera={{position:[0,0,3.2], fov:55}} dpr={[1,2]} gl={{antialias:true, alpha:true}}> com <color attach="background" args={["#fefcf8"]} />; luzes: ambientLight 0.8, directionalLight pos [3,5,2] intensity 1.2, pointLight pos [-3,-2,3] intensity 0.6 color #0e4a7a. OrbitControls enableZoom true enablePan false minDistance 1.5 maxDistance 6.
    5. Labels: Text para "He²⁺" acima do núcleo, "e⁻" próximo a cada elétron via Html ou Text com fontSize 0.12 color #0e4a7a; legenda orbital "Órbita 1s² → 1s⁰ (He²⁺ sem elétrons — modelo didático com elétrons para contraste)".
    6. Performance: NÃO usar useState para posição (evitar 60 re-renders); apenas useFrame muta ref diretamente — já validado em RESEARCH anti-pattern #2.
    7. Export AtomicStructure3D mantém assinatura, mas Canvas agora tem className "w-full h-full" e style transparente.
    Evitar: useState para animação, múltiplos Canvas, OrbitControls sem limites.
  </action>
  <verify>
    <automated>grep -n "useFrame\|useRef\|OrbitControls\|Text\|Html\|clock.elapsedTime" src/components/AtomicStructure3D.jsx | head -n 40; npx vite build 2>&1 | grep -i "error" | head -n 20</automated>
  </verify>
  <done>AtomicStructure3D.jsx contém useFrame com clock.elapsedTime atualizando posição de elétrons (grep hit), OrbitControls com limites, luzes ambient+directional+point, labels He²⁺/e⁻/Núcleo, Canvas bg #fefcf8, sem useState para animação; build passa.</done>
</task>

<task type="auto">
  <name>Task 5: ParticleSimulation2D — câmara de ionização interativa</name>
  <files>src/components/ParticleSimulation2D.jsx</files>
  <action>
    Fix: ParticleSimulation2D.jsx:10 atual é canvas estático com 3 bodies isStatic, grid cinza #fafafa, sem interatividade, sem conceito de câmara de ionização.
    Reimplementar como câmara de ionização didática interativa (detector de fumaça):
    1. Manter estrutura Matter.js com render:{visible:false} (pitfall #2) e requestAnimationFrame draw loop; mas evoluir física:
       - Criar câmara retangular: walls estáticas (4 bodies retangulares) formando câmara com inset; eletrodos (cátodo/ânodo) como retângulos estáticos coloridos (#0e4a7a e #c9a227).
       - Fonte alfa: representação Am-241 como pequeno círculo #ff6b6b estático emitindo partículas alfa (bodies circulares pequenos com label "alpha" e velocity inicial).
       - Íons He²⁺/ar ionizado: bodies leves que derivam para eletrodos; elétrons livres: bodies minúsculos azul #4fc3f7 com massa 0.001.
       - Fumaça: ao clicar botão ou canvas, spawnar bodies "smoke" cinza grandes que capturam íons (colisão -> remover íon, reduzir corrente).
    2. Interatividade (requisito):
       - Botão "Inserir Fumaça" (fora do canvas mas dentro do componente) dispara spawnSmoke(n=12) com Bodies.circle random x,y + frictionAir 0.02.
       - Clique/drag no canvas: Matter.Mouse + MouseConstraint para arrastar partículas; distinguir short click vs drag não é crítico aqui (não compete com Spectacle navigation porque tabs isolam).
       - Mostrar corrente iônica simulada: calcular ions livres count -> exibir gauge "Corrente: XX µA — Alarme: ON/OFF" (corrente cai com fumaça).
       - Controles: Reset, Toggle Alfa ON/OFF.
    3. Visual: canvas bg #fefcf8, grid sutil #0e4a7a/5, câmara border #c9a227, labels com fillText: "Fonte α (²⁴¹Am)", "Cátodo (-)", "Ânodo (+)", "Câmara de Ionização". Bodies draw com cores revista: proton/ion #c9a227, electron #0e4a7a, smoke #9ca3af, alpha #ef4444.
    4. Draw loop: manter Matter.Engine.update(engine, 1000/60) + ctx.clearRect + ctx.fillRect #fefcf8 + grid + chamber outline + bodies iteration; NÃO usar useState para physics — usar refs e requestAnimationFrame.
    5. Resize: manter resizeHandler que atualiza canvas.width/height via container.clientWidth; adicionar debounce 150ms.
    6. Cleanup: isolar engine por instância (não shared), dispose em return () => { running=false; cancelAnimationFrame; Matter.Engine.clear; Matter.World.clear } — pitfall #10.
    7. Acessibilidade: canvas com role img aria-label "Simulação 2D da câmara de ionização — interativa".
    Evitar: Matter.Render.create (conflita com Three.js), bodies isStatic para tudo (precisa dinâmica para fumaça/íons), useState para loop.
  </action>
  <verify>
    <automated>grep -n "Matter\.\|spawnSmoke\|MouseConstraint\|requestAnimationFrame\|Corrente\|Câmara" src/components/ParticleSimulation2D.jsx | head -n 50</automated>
  </verify>
  <done>ParticleSimulation2D contém câmara com walls/eletrodos, spawnSmoke interativo, MouseConstraint para drag, gauge de corrente, botões Inserir Fumaça/Reset/Toggle, draw loop via requestAnimationFrame sem useState, resize debounced, cleanup correto; visual em #fefcf8/#0e4a7a/#c9a227.</done>
</task>

<task type="auto">
  <name>Task 6: App.jsx + content slides — revista ilustrada com Figure/fotos</name>
  <files>src/components/App.jsx, src/components/ChemicalInfo.jsx, src/components/References.jsx, src/components/ui/figure.jsx</files>
  <action>
    App.jsx:7 atual é Deck bare com 7 Slides crus (h1, p, ul sem estilo). Reestruturar para revista científica ilustrada mantendo Deck como orquestrador:
    1. App.jsx — envolver Deck em <div className="min-h-screen bg-[#fefcf8]">; Deck props: theme light, template com progress bar dourada; manter 7 slides mas reordenar e enriquecer:
       - Slide 1 Título: hero revista — kicker "Química Geral • Revista Científica Ilustrada", h1 grande serif "Detectores de Fumaça e a Descoberta do Núcleo Atômico", subtítulo "Do experimento de Rutherford ao sensor da sua casa — a jornada do íon He²⁺", Figure foto real detector de fumaça (src placeholder Unsplash/Wikipedia commons com caption "Detector iônico residencial — fonte ²⁴¹Am ioniza o ar" credit), badge "He²⁺ • Z=2 • Cátion +2".
       - Slide 2 Identificação Química: usar ChemicalInfo.jsx estilizado (cards com border gold, bg white, grid 2 col) + Figure estrutura atômica He.
       - Slide 3 Estrutura Subatômica: texto + diagrama Figure prótons/elétrons + callout "He → He²⁺ + 2e⁻".
       - Slide 4 Imagem Representativa: substituir p genérico por Figure grande do íon He²⁺ com caption didática + nota "Representação artística — escala não real".
       - Slide 5 Autoria: card centralizado revista com nome/turma/disciplina/professor/data — estilo certificado.
       - Slide 6 Simulações: <SimulationsIntegration/> já com tabs — adicionar contexto textual acima: "Explore os dois modelos: estrutura 3D e câmara 2D".
       - Slide 7 Referências: References.jsx estilizado (lista numerada com links, tipografia small).
       - Opcional Slide 8 extra se couber: Aplicações He²⁺ parágrafo (SLD-07) — garantir SLD-07 coberto.
    2. ChemicalInfo.jsx — reestilizar: cards Tailwind grid, ion symbol grande serif gold, Z=2 badge blue, table propriedades; manter leitura de ION_PROPERTIES.
    3. References.jsx — reestilizar: lista com hanging indent, links clicáveis, seção "Para saber mais" com QR placeholder; manter citações Rutherford/NFPA/IAEA.
    4. Figure uso: todos placeholders com src real (Unsplash/Wikipedia) e alt descritivo; nunca quebrar se imagem falha (onError fallback gradient).
    5. Garantir todos SLD-01..09 mapeados: SLD-01 título, SLD-02 ChemicalInfo, SLD-03 subatômica, SLD-04 Figure He²⁺, SLD-07 parágrafo aplicações (adicionar parágrafo dedicado), SLD-08 autoria, SLD-09 referências, SLD-05/06 via simulações.
    Evitar: remover Deck (manter orquestrador), hardcode fotos sem caption, deixar Slide 4 apenas com <p>.
  </action>
  <verify>
    <automated>grep -n "Figure\|ChemicalInfo\|SimulationsIntegration\|References\|He²⁺\|Detectores" src/components/App.jsx | head -n 60; grep -n "className.*revista\|#fefcf8\|#0e4a7a" src/components/ChemicalInfo.jsx src/components/References.jsx 2>&1 | head -n 20</automated>
  </verify>
  <done>App.jsx contém 7-8 Slides enriquecidos com Figure fotos reais com caption/credit, ChemicalInfo cards revista, Simulações com contexto, References estilizada; todos SLD-01..09 cobertos; Deck mantido como orquestrador; nenhum Slide bare.</done>
</task>

<task type="auto">
  <name>Task 7: Build + Playwright verification (canvas count, console errors)</name>
  <files>playwright.config.js, tests/e2e/simulations.spec.js, package.json</files>
  <action>
    Verificação final — fechar loop Wave 2:
    1. package.json — adicionar script "test:e2e": "playwright test" e devDependency @playwright/test latest (npx playwright install --with-deps chromium). Garantir "build": "vite build" já existe.
    2. playwright.config.js — criar na raiz: import { defineConfig } from "@playwright/test"; export default defineConfig({ testDir:"tests/e2e", webServer:{command:"npx vite --port 4173 --strictPort", url:"http://localhost:4173/detectores-fumaca/", reuseExistingServer:false }, use:{ baseURL:"http://localhost:4173/detectores-fumaca/" }, timeout:30000 });
       Ajustar baseURL conforme vite.config base "/detectores-fumaca/" — Playwright deve navegar para /detectores-fumaca/.
    3. tests/e2e/simulations.spec.js — criar 3 testes:
       a) "build passes": executa npx vite build via execSync e asseteia exit 0 (ou apenas checa dist/index.html existe).
       b) "simulações visíveis — canvas count": page.goto("/"), aguardar Deck carregar, navegar para slide Simulações (click next 5x ou goto "#/5" se Spectacle usa hash), esperar 2s, avaliar document.querySelectorAll('canvas').length >=2 (um do Three.js, um do 2D) e canvas visíveis (offsetParent !== null ou getBoundingClientRect width>0). Falha se <2.
       c) "no console errors": page.on('console', msg=> se msg.type()==='error' e não é warning conhecido de three, falhar); também page.on('pageerror', err=> falhar). Navegar por todos slides e verificar zero pageerror.
    4. Rodar localmente: npx vite build (deve passar) e npx playwright test --reporter=list (deve passar 3 testes). Se falhar por base path, ajustar webServer url para http://localhost:4173/.
    5. Documentar no SUMMARY: incluir evidências — screenshot Playwright se possível.
    Evitar: Playwright sem webServer (flaky), teste apenas de título, ignorar console errors.
  </action>
  <verify>
    <automated>npx vite build 2>&1 | tail -n 30; npx playwright test --reporter=list 2>&1 | tail -n 50</automated>
  </verify>
  <done>npx vite build passa (dist gerado); playwright.config.js existe; tests/e2e/simulations.spec.js com 3 testes (build, canvas >=2 visíveis, no console errors) todos PASS; evidência de canvas count >=2 no slide Simulações.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Canvas (WebGL/2D) | Código de simulação executa no cliente; sem dados sensíveis, mas falhas de WebGL podem quebrar UX |
| NPM supply chain -> Build | Bump de 7 deps + Tailwind 4.1 — risco de supply chain / breaking change |
| User interaction -> Physics engine | MouseConstraint permite input arbitrário que alimenta Matter.js |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | vite.config.js / @tailwindcss/vite | medium | mitigate | Pin @tailwindcss/vite 4.1.x exact, verificar build hash, não auto-update major |
| T-02-02 | Denial of Service | AtomicStructure3D useFrame | low | mitigate | Clamp dpr [1,2], limitar OrbitControls distance, não alocar geometria por frame |
| T-02-03 | Denial of Service | ParticleSimulation2D spawnSmoke | medium | mitigate | Limitar spawn para max 30 bodies, throttled click, World.clear no reset |
| T-02-04 | Spoofing | Figure photos / external URLs | low | mitigate | Usar apenas Wikimedia/Unsplash https, alt sempre, onError fallback, não executar JS de src |
| T-02-05 | Information Disclosure | Console errors leaking paths | low | accept | Projeto educacional sem dados sensíveis; Playwright apenas verifica absence de pageerror |
| T-02-SC | Tampering | npm installs (react, three, vite, tailwind) | high | mitigate | Verificar cada pacote em npmjs.com antes de install; checkpoint humano se [ASSUMED]/[SUS]; slopcheck antes de commit |
</threat_model>

<verification>
- npx vite build passa sem erros (dist/index.html + assets gerados)
- grep -c "IntersectionObserver" src/components/SimulationsIntegration.jsx == 0
- grep -c "useFrame" src/components/AtomicStructure3D.jsx >=1 e electrons animados (inspecionar clock.elapsedTime)
- document.querySelectorAll('canvas').length >=2 no slide Simulações (Playwright)
- page.on('pageerror') zero erros ao navegar todos slides (Playwright)
- Visual: fundo #fefcf8, headings #0e4a7a, accents #c9a227 presentes (grep tokens)
- Fotos com <figcaption> presentes em App.jsx (>=3 Figures)
</verification>

<success_criteria>
- Simulações aparecem imediatamente ao entrar no slide (tabs 3D|2D, sem gate)
- Elétrons orbitam continuamente (não estáticos) com labels e luzes revista
- Câmara de ionização 2D interativa com Inserir Fumaça, drag, gauge de corrente
- Deck Spectacle mantido como orquestrador, slides com estilo revista científica ilustrada
- Fotos reais com legenda em todos slides de conteúdo
- Dependências bumped para latest compat (react 19.2, fiber 9, drei 10, three 0.185+, vite 8, tailwind 4.1)
- Build + Playwright (canvas >=2, no console errors) verdes
</success_criteria>

<output>
Create .planning/phases/02-build/02-02-SUMMARY.md when done; update .planning/ROADMAP.md Wave 2 status
</output>
